{
    "require-dev": {
        "phpunit/phpunit": "5.7",
        "phpunit/dbunit": "^2.0",
        "phpspec/phpspec": "^3.4"
    },

    "scripts": {
  		"tests": "phpunit ./tests",
		"specs": "phpspec run --format=dot"
	},

    "config": {
        "bin-dir": "bin"
    },
    "autoload": {"psr-0": {"": "src"}},
    "require": {
        "silex/silex": "~2.0"
    }
}

