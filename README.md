"# work-exercise" 

Work exercises.


Run all tests:
'composer tests'

run custom test:
composer exec "phpunit ./tests/mytest.php"

Run SPECS:
'composer specs'

run custom spec:
bin\phpspec run --format=dot //WIP TODO!!!


<doh>